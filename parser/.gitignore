# Репозиторий публичный — сюда не должно попасть ничего тяжёлого и ничего личного.

# База каталога (~238 МБ) и всё, что из неё генерируется.
data/
*.db
*.db-wal
*.db-shm
*.db.backup
*.sql
*.sql.gz

# Виртуальное окружение с psycopg.
.venv/
__pycache__/
*.pyc

# Дампы и выгрузки на всякий случай.
*.jsonl
